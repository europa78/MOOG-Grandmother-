import React from "react";
import ReactDOM from "react-dom/client";
import DOOMStepMother from "./DOOMStepMother";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DOOMStepMother />
  </React.StrictMode>
);
